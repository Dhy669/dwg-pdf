# DWG 转 PDF 运行说明

以下命令需要在 Windows PowerShell 中运行。

## 1. 进入项目目录

```powershell
cd "D:\项目\dwg文件处理"
```

## 2. 单个 DWG 测试

下面使用项目中实际存在的文件 `E-SP-(LV+MV)-JM1.dwg` 作为示例：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File "D:\项目\dwg文件处理\run.ps1" `
  -InputRoot "D:\项目\dwg文件处理\RCJM1   MEP图纸汇总---20260617" `
  -OutputRoot "D:\项目\dwg文件处理\PDF测试输出" `
  -ExactFilePath "D:\项目\dwg文件处理\RCJM1   MEP图纸汇总---20260617\20260617 RCJM1 电气专业图纸CAD\20260617 RCJM1 电气专业图纸CAD\Alpha\ELECT\RCJM1-CL-CAD\E-SP-(LV+MV)-JM1.dwg" `
  -SkipBuild
```

`-SkipBuild` 表示直接使用已经编译好的 DLL。如果修改了 `Plugin.cs` 且尚未重新编译，请删除这一行，同时删除上一行末尾的反引号；脚本会先编译再运行。

## 3. 转换全部 DWG

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File "D:\项目\dwg文件处理\run-all.ps1" `
  -InputRoot "D:\项目\dwg文件处理\RCJM1   MEP图纸汇总---20260617" `
  -OutputRoot "D:\项目\dwg文件处理\PDF输出_按原目录"
```

输出目录会保持与 DWG 源目录相同的文件夹层级。

## 4. 注意事项

- PowerShell 的反引号 `` ` `` 必须是该行最后一个字符，后面不能有空格或其他文字。
- `-ExactFilePath` 必须填写真实存在的 `.dwg` 文件，不能填写“实际目录”或“实际图纸.dwg”等占位文字。
- 单文件测试日志：`D:\项目\dwg文件处理\DwgBatchPdf\bin\Release\batch.log`。
- 全量运行进度日志：`D:\项目\dwg文件处理\batch-all.log`。
